(async()=>{const box=document.getElementById('productDetails');const id=new URLSearchParams(location.search).get('id');if(!id){box.innerHTML='<p>Product not found.</p>';return}try{const res=await fetch(`/api/products/${id}`);const p=await res.json();if(!res.ok)throw new Error(p.message);box.innerHTML=`<img src="${p.image}" alt="${p.name}"><div><span class="category">${p.category}</span><h1>${p.name}</h1><div class="price">₹${p.price.toLocaleString('en-IN')}</div><p class="description">${p.description}</p><p>Stock available: ${p.stock}</p><br><button id="add" class="btn primary">Add to Cart</button></div>`;document.getElementById('add').onclick=()=>addToCart(p)}catch(e){box.innerHTML=`<p>${e.message}</p>`}})();
